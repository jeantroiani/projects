"use strict";

class StringCalc {

    constructor (string) {
        this.string = string;
    }

    add() {
        if (this.string == null) {return 0}
        else {
            return (Number(string));
        }
    }
}
